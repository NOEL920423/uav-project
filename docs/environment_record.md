# Environment Record

## System

```bash
uname -a
lsb_release -a
nvidia-smi
python3 --version

which python3
pip --version
printenv | grep ROS
ros2 --help
cd ~/PX4-Autopilot
git remote -v
git branch
git rev-parse HEAD
cd ~/PegasusuSimulator
git remote -v
git branch
git rev-parse HEAD
